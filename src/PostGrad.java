import javax.swing.*;

public class PostGrad extends Student {
    private String supervisor;
    private String category;

    public PostGrad (String name, int age){
        setAge(age);
        setName(name);
    }
}
